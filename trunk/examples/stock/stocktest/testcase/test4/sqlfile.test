[default]

[start]
calltest: setup
sqlexception: CREATE TABLE TESTSUMMARY(ITEM BIGINT NOT NULL, AMOUNT DECIMAL(15, 4))
sql:DELETE FROM TESTSUMMARY
sql: INSERT INTO TESTSUMMARY (SELECT ID,NULL FROM ITEM);

[verify]
verify:select COUNT(*) AS C from (select itemid, sum(currentamount) as a from itemdashboard group by itemid) full outer join (select * from testsummary where amount is not null and amount > 0) on itemid = item where a != amount
equal C 0

