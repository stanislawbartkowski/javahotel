<?xml version="1.0"?>
<typedefs xmlns="http://www.jythongwt.typedefxsd.com">

     <jython>
       <import>from schedpack import sched</import>
      </jython>    

  <typedef id="datasources" type="combo" comboid="id" displayname="name" >
  
     <jython>
        <method>sched.connectiontypeaction({0},{1})</method>
      </jython>    
 
   </typedef>

  <typedef id="schemas" type="combo" comboid="id">
  
     <jython>
        <method>sched.schemasaction({0},{1})</method>
      </jython>    
 
   </typedef>

  <typedef id="spprocs" type="helper" comboid="procname">
  
     <jython>
        <import>from schedpack import proclist</import>
        <method>proclist.dialogaction({0},{1})</method>
      </jython>    
 
   </typedef>

  
</typedefs>